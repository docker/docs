Test upload and download via the docker socket
==============================================

This test times uploading and downloading large blank files over the
docker socket as used by `docker build` and `docker cp`
