--- a/retry-without-creating-duplicates.md
+++ b/retry-without-creating-duplicates.md
@@ -31,5 +31,10 @@
       'shell',
       { displayName, resources: { cpus: 2, memoryMib: 4096 } },
-      { idempotencyKey: requestId, signal, maxRetries: 0 },
+      {
+        idempotencyKey: requestId,
+        timeoutMs: 300_000,
+        signal,
+        maxRetries: 0,
+      },
     );
   } catch (error) {
@@ -64,5 +69,10 @@
         'shell',
         { displayName, resources: { cpus: 2, memoryMib: 4096 } },
-        { idempotencyKey: requestId, signal, maxRetries: 0 },
+        {
+          idempotencyKey: requestId,
+          timeoutMs: 300_000,
+          signal,
+          maxRetries: 0,
+        },
       );
     } catch (error) {
