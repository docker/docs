--- a/give-a-sandbox-an-mcp-gateway.md
+++ b/give-a-sandbox-an-mcp-gateway.md
@@ -24,9 +24,13 @@

 ```typescript
-const sandbox = await client.kits.launch(kitName, {
-  resources: { cpus: 2, memoryMib: 4096 },
-  mcp: { servers, static: true },
-  storage: { secrets },
-});
+const sandbox = await client.kits.launch(
+  kitName,
+  {
+    resources: { cpus: 2, memoryMib: 4096 },
+    mcp: { servers, static: true },
+    storage: { secrets },
+  },
+  { timeoutMs: 300_000 },
+);
 return sandbox.waitUntilRunning();
 ```
@@ -44,9 +48,13 @@
   secrets: string[] = [],
 ) {
-  const sandbox = await client.kits.launch(kitName, {
-    resources: { cpus: 2, memoryMib: 4096 },
-    mcp: { servers, static: true },
-    storage: { secrets },
-  });
+  const sandbox = await client.kits.launch(
+    kitName,
+    {
+      resources: { cpus: 2, memoryMib: 4096 },
+      mcp: { servers, static: true },
+      storage: { secrets },
+    },
+    { timeoutMs: 300_000 },
+  );
   return sandbox.waitUntilRunning();
 }
