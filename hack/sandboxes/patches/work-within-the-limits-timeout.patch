--- a/work-within-the-limits.md
+++ b/work-within-the-limits.md
@@ -90,5 +90,10 @@
       'shell',
       { displayName, resources: { cpus: 2, memoryMib: 4096 } },
-      { idempotencyKey: requestId, signal, maxRetries: 0 },
+      {
+        idempotencyKey: requestId,
+        timeoutMs: 300_000,
+        signal,
+        maxRetries: 0,
+      },
     );
   } catch (error) {
@@ -131,5 +136,10 @@
         'shell',
         { displayName, resources: { cpus: 2, memoryMib: 4096 } },
-        { idempotencyKey: requestId, signal, maxRetries: 0 },
+        {
+          idempotencyKey: requestId,
+          timeoutMs: 300_000,
+          signal,
+          maxRetries: 0,
+        },
       );
     } catch (error) {
