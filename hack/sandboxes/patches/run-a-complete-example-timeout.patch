--- a/run-a-complete-example.md
+++ b/run-a-complete-example.md
@@ -53,5 +53,5 @@
   let sandbox: Sandbox | undefined;
   try {
-    const operation = { signal: AbortSignal.timeout(300_000) };
+    const operation = { timeoutMs: 300_000 };
     sandbox = await client.kits.launch(
       'shell',
