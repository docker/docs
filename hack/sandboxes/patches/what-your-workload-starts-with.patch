--- a/what-your-workload-starts-with.md
+++ b/what-your-workload-starts-with.md
@@ -26,5 +26,5 @@
 export async function readFloor(sandbox: Sandbox) {
   const result = requireSuccess(
-    await sandbox.processes.run({ args: ['env'] }),
+    await sandbox.processes.run({ args: ['env'] }, { timeoutMs: 300_000 }),
   );
   return Object.fromEntries(
@@ -48,5 +48,5 @@
 export async function readFloor(sandbox: Sandbox) {
   const result = requireSuccess(
-    await sandbox.processes.run({ args: ['env'] }),
+    await sandbox.processes.run({ args: ['env'] }, { timeoutMs: 300_000 }),
   );
   return Object.fromEntries(
@@ -78,12 +78,22 @@
 ```typescript
 const fromRequest = requireSuccess(
-  await sandbox.processes.run({
+  await sandbox.processes.run(
+    {
+      args: ['printenv', name],
+      env: { [name]: value },
+    },
+    { timeoutMs: 300_000 },
+  ),
+);
+const fromSandbox = await sandbox.processes.run(
+  {
     args: ['printenv', name],
-    env: { [name]: value },
-  }),
+  },
+  { timeoutMs: 300_000 },
 );
-const fromSandbox = await sandbox.processes.run({
-  args: ['printenv', name],
-});
+return {
+  fromRequest: fromRequest.stdout.trimEnd(),
+  fromSandbox: fromSandbox.stdout.trimEnd(),
+};
 ```

@@ -100,12 +110,18 @@
 ) {
   const fromRequest = requireSuccess(
-    await sandbox.processes.run({
+    await sandbox.processes.run(
+      {
+        args: ['printenv', name],
+        env: { [name]: value },
+      },
+      { timeoutMs: 300_000 },
+    ),
+  );
+  const fromSandbox = await sandbox.processes.run(
+    {
       args: ['printenv', name],
-      env: { [name]: value },
-    }),
+    },
+    { timeoutMs: 300_000 },
   );
-  const fromSandbox = await sandbox.processes.run({
-    args: ['printenv', name],
-  });
   return {
     fromRequest: fromRequest.stdout.trimEnd(),
